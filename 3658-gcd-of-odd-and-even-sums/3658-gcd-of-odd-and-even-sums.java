class Solution {
    public int gcdOfOddEvenSums(int n) {
        // double oddSum = Math.pow(n,2);
        // double evenSum = Math.pow(n,2) + n;
        // int gcd = 1;
        // for(int i = 2;i<=oddSum;i++){
        //     if(oddSum%i==0 && evenSum%i==0) gcd = i;
        // }
        // return gcd;

        return n;
    }
}